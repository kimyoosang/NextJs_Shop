import React from 'react';

interface AddToCartWidgetProps {
  productId: number;
}
const AddToCartWidget: React.FC<AddToCartWidgetProps> = ({ productId }) => {
  return <div>AddToCartWidget</div>;
};

export default AddToCartWidget;
